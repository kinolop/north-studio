# North Agent — assets

The case pages no longer carry background plates or image panels inside
cards: sections stand on the site's own near-black, and the cards are flat
dark panels with a hairline. The only image this page reads is the mascot.

| File | Ratio | Where it appears |
| --- | --- | --- |
| `mascot.png` | ~1:1 | The chrome figure in the hero. Also read by the North Flow hero |

`hero.png`, `cap-answers.png`, `cap-knows.png` and `cap-enroll.png` are
still in this folder but nothing renders them — they were the hero plate and
the three capability panels, both removed. Delete them whenever you like, or
leave them here in case that decision is reversed.
