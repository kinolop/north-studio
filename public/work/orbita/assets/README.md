# ORBITA — assets

Nothing to drop. The brand mark, the orbital motif and all three app screens
are drawn in code, and the sections stand on the site's own near-black.

The three feature frames still accept `shot-1.png`, `shot-2.png` and
`shot-3.png` (4:5) if you ever want renders instead of the coded screens —
while they are absent the live mock renders, which is the intended state.
Keep any you make in ORBITA's palette: cold aqua on near-black, `#4ec9dc`.
